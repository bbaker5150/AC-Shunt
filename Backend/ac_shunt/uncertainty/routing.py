"""
WebSocket routes for the Uncertainty Budget module.

Concatenated into the project ASGI router under the ``ws/uncertainty/``
namespace (wired in a later step). Empty until the module needs live updates.
"""
websocket_urlpatterns = []
