"""
WebSocket routes for the Report of Calibration module.

Concatenated into the project ASGI router under the ``ws/reports/`` namespace
(wired in a later step). Empty until the module needs live updates.
"""
websocket_urlpatterns = []
