"""
Channels consumers for the Report of Calibration module.

None yet — consumers are added here and registered in ``routing.py`` when the
module needs WebSocket push (e.g. live report generation progress).
"""
